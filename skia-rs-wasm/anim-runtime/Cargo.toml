[package]
name = "anim-runtime"
version = "0.1.0"
edition = "2021"
description = "Portable animation runtime core (Rust port of the skia-rs-wasm anim/ IR)"
license-file = "../LICENSE"

[lib]
name = "anim_runtime"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
