# Template only. Copy to secret.yaml, fill in real values, apply, and DO NOT commit.
apiVersion: v1
kind: Secret
metadata:
  name: ingest-pipeline-secrets
  labels:
    app.kubernetes.io/part-of: ingest-pipeline
type: Opaque
stringData:
  DB_USERNAME: "postgres"
  DB_PASSWORD: "REPLACE_ME"

  MINIO_ROOT_USER: "REPLACE_ME"
  MINIO_ROOT_PASSWORD: "REPLACE_ME"

  AUTH_FIGMA_CLIENT_ID: "REPLACE_ME"
  AUTH_FIGMA_CLIENT_SECRET: "REPLACE_ME"
  AUTH_GITHUB_CLIENT_ID: "REPLACE_ME"
  AUTH_GITHUB_CLIENT_SECRET: "REPLACE_ME"

  OPENAI_API_KEY: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
  GEMINI_API_KEY: "REPLACE_ME"
