[project]
name = "figma-mock"
version = "0.1.0"
description = "This is a small mock server for Figma API endpoints"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.116.0",
    "uvicorn>=0.35.0",
    "python-multipart>=0.0.9", # For handling form data
]